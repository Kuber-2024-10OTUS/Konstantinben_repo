apiVersion: v1
kind: Service
metadata:
  name: hw-service
  namespace: homework
  labels:
    app: nginx
spec:
  selector:
    app: nginx
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
    - port: 9113
      targetPort: 9113
      name: nginx-metrics